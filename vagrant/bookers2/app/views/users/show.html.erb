

    <%= render 'books/kyoutu', book:@book ,user: @user %>


        <div class="col-xs-9">
            <h2>Books</h2>

            <table class="table table-hover table-inverse">
                <thead>
                    <tr>
                        <th></th>
                        <th>Title</th>
                        <th>Opinion</th>
                    </tr>
                </thead>
                    <% @books.each do |book| %>
                    <tr>
                        <td><%= link_to (current_user) do %><%= attachment_image_tag @user, :profile_image, :fill, 40, 40, class: " pull-left profile-thumb", fallback: "no_image.jpg" %><%end%></td>
                        <td><%= link_to book.title,(book_path(book.id)) %></td>
                        <td><%= book.body %></td>
                    </tr>
               
                <% end %>
            </table>
        </div>
        <footer>
            <div class="container">
                <div class="row">
                    <div class="col-xs-3 col-xs-offset-9">
                        <p>CopyRight Infratop.inc</p>
                    </div>
                </div>
            </div>
        </footer>
    </div>
</div>

